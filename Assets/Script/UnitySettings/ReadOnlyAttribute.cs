using UnityEngine;

public class ReadOnlyAttribute : PropertyAttribute
{
    // このクラスは、Unityエディタでフィールドを読み取り専用にするための属性を定義します。
    // Unityエディタでのカスタムインスペクターを作成することで、フィールドを読み取り専用にすることができます。
    // ただし、Unityの標準機能では、フィールドを完全に読み取り専用にすることはできません。
    // 代わりに、カスタムインスペクターを使用して、フィールドを表示するだけで編集できないようにします。
}
